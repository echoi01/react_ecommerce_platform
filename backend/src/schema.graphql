#imp0rt made possible via graphql-import
# import * from './generated/prisma.graphql'

type Mutation {
    createItem(title: String, descripition: String, Price: Int,
    image: String, largeImage: String): Item!
}

type Query {
    items: [Item]!
}